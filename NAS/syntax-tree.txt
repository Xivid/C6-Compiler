FOR 
|    |    |     |
|	 |    |     |
stmt stmt stmt stmt
eg.
for (i=0 i<10 i++) print i
ex i=0
L001 :
ex i< 10 
j0 L002
ex print i
ex i++
jmp L001
L002：

WHILE expr stmt
eg.
while (i<10) i++
L001:
ex i<0
j0 L002
ex i++
jmp L001
L002:

IF expr stmt 
IF expr stmt ELSE stmt
//if has else 
eg. if (x<y) x else y
ex x<y 
j0 L001
ex x 
jmp L002
L001:
ex y
L002:
//no else
eg. if (x<y) x
ex x<y
j0 L001
ex x 
L001:

READ var
eg. read x (get x from input)
geti
//save the position var++

//we need to be able to access the value of x 
//environment?

//type systems?

PRINT expr 
eg print x+1
ex x+1
puti/putc/puts
//switch according to the type of the result

var = expr
eg x=x+1
ex x+1
//if x is stored in fp[0]
pop fp[0]

2 issues:
saving variables in an environment 
type system

Environment 
[(varname,fp subscript,type (int,char,string))]
(strings are immutable will not appear in = statement)
hash table?

in a function call 
automatically create an environment according to the params 

symbol table 
implement a hash table

